# Optional: used by scripts/build_install_macos.sh to re-sign the built app
# Find identities with: security find-identity -v -p codesigning
MEIKIKAI_CODESIGN_IDENTITY="Apple Development: you@example.com (TEAMID)"
